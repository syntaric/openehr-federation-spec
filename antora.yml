name: federation-aql
title: Federation Tier with AQL
# Stays at the minor version: a patch release replaces its predecessor in place,
# so there is no version selector and no stale URLs. The patch level lives in
# the display attributes below.
version: '0.3'
start_page: index.adoc
nav:
  - modules/ROOT/nav.adoc
asciidoc:
  attributes:
    spec-version: '0.3.1'
    spec-status: 'Draft for review'
    spec-date: '2026-09-11'
